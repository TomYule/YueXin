<template>
    <div class="row justify-content-center">
        <div class="col-8">
            <form name="editForm" role="form" novalidate v-on:submit.prevent="save()" >
                <h2 id="yueXinApp.${className}.home.createOrEditLabel" v-text="$t('yueXinApp.${className}.home.createOrEditLabel')">Create or edit a ${className}</h2>
                <div>
                    <div class="form-group" v-if="${className}.id">
                        <label for="id" v-text="$t('global.field.id')">ID</label>
                        <input type="text" class="form-control" id="id" name="id"
                               v-model="${className}.id" readonly />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.jobName')" for="sys-job-jobName">Job Name</label>
                        <input type="text" class="form-control" name="jobName" id="sys-job-jobName"
                               :class="{'valid': !$v.${className}.jobName.$invalid, 'invalid': $v.${className}.jobName.$invalid }" v-model="$v.${className}.jobName.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.jobGroup')" for="sys-job-jobGroup">Job Group</label>
                        <input type="text" class="form-control" name="jobGroup" id="sys-job-jobGroup"
                               :class="{'valid': !$v.${className}.jobGroup.$invalid, 'invalid': $v.${className}.jobGroup.$invalid }" v-model="$v.${className}.jobGroup.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.invokeTarget')" for="sys-job-invokeTarget">Invoke Target</label>
                        <input type="text" class="form-control" name="invokeTarget" id="sys-job-invokeTarget"
                               :class="{'valid': !$v.${className}.invokeTarget.$invalid, 'invalid': $v.${className}.invokeTarget.$invalid }" v-model="$v.${className}.invokeTarget.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.cronExpression')" for="sys-job-cronExpression">Cron Expression</label>
                        <input type="text" class="form-control" name="cronExpression" id="sys-job-cronExpression"
                               :class="{'valid': !$v.${className}.cronExpression.$invalid, 'invalid': $v.${className}.cronExpression.$invalid }" v-model="$v.${className}.cronExpression.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.misfirePolicy')" for="sys-job-misfirePolicy">Misfire Policy</label>
                        <input type="text" class="form-control" name="misfirePolicy" id="sys-job-misfirePolicy"
                               :class="{'valid': !$v.${className}.misfirePolicy.$invalid, 'invalid': $v.${className}.misfirePolicy.$invalid }" v-model="$v.${className}.misfirePolicy.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.status')" for="sys-job-status">Status</label>
                        <input type="text" class="form-control" name="status" id="sys-job-status"
                               :class="{'valid': !$v.${className}.status.$invalid, 'invalid': $v.${className}.status.$invalid }" v-model="$v.${className}.status.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.createBy')" for="sys-job-createBy">Create By</label>
                        <input type="text" class="form-control" name="createBy" id="sys-job-createBy"
                               :class="{'valid': !$v.${className}.createBy.$invalid, 'invalid': $v.${className}.createBy.$invalid }" v-model="$v.${className}.createBy.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.createTime')" for="sys-job-createTime">Create Time</label>
                        <b-input-group class="mb-3">
                            <b-input-group-prepend>
                                <b-form-datepicker
                                    aria-controls="sys-job-createTime"
                                    v-model="$v.${className}.createTime.$model"
                                    name="createTime"
                                    class="form-control"
                                    :locale="currentLanguage"
                                    button-only
                                    today-button
                                    reset-button
                                    close-button
                                >
                                </b-form-datepicker>
                            </b-input-group-prepend>
                            <b-form-input id="sys-job-createTime" type="text" class="form-control" name="createTime"  :class="{'valid': !$v.${className}.createTime.$invalid, 'invalid': $v.${className}.createTime.$invalid }"
                                          v-model="$v.${className}.createTime.$model"  />
                        </b-input-group>
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.updateBy')" for="sys-job-updateBy">Update By</label>
                        <input type="text" class="form-control" name="updateBy" id="sys-job-updateBy"
                               :class="{'valid': !$v.${className}.updateBy.$invalid, 'invalid': $v.${className}.updateBy.$invalid }" v-model="$v.${className}.updateBy.$model" />
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.upLocalDate')" for="sys-job-upLocalDate">Up Local Date</label>
                        <b-input-group class="mb-3">
                            <b-input-group-prepend>
                                <b-form-datepicker
                                    aria-controls="sys-job-upLocalDate"
                                    v-model="$v.${className}.upLocalDate.$model"
                                    name="upLocalDate"
                                    class="form-control"
                                    :locale="currentLanguage"
                                    button-only
                                    today-button
                                    reset-button
                                    close-button
                                >
                                </b-form-datepicker>
                            </b-input-group-prepend>
                            <b-form-input id="sys-job-upLocalDate" type="text" class="form-control" name="upLocalDate"  :class="{'valid': !$v.${className}.upLocalDate.$invalid, 'invalid': $v.${className}.upLocalDate.$invalid }"
                                          v-model="$v.${className}.upLocalDate.$model"  />
                        </b-input-group>
                    </div>
                    <div class="form-group">
                        <label class="form-control-label" v-text="$t('yueXinApp.${className}.remark')" for="sys-job-remark">Remark</label>
                        <textarea class="form-control" name="remark" id="sys-job-remark"
                                  :class="{'valid': !$v.${className}.remark.$invalid, 'invalid': $v.${className}.remark.$invalid }" v-model="$v.${className}.remark.$model" ></textarea>
                    </div>
                </div>
                <div>
                    <button type="button" id="cancel-save" class="btn btn-secondary" v-on:click="previousState()">
                        <font-awesome-icon icon="ban"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.cancel')">Cancel</span>
                    </button>
                    <button type="submit" id="save-entity" :disabled="$v.${className}.$invalid || isSaving" class="btn btn-primary">
                        <font-awesome-icon icon="save"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.save')">Save</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</template>
<script lang="ts" src="./sys-job-update.component.ts">
</script>
